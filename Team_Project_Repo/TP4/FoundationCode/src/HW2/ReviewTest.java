package HW2;
import static org.junit.Assert.*;
import org.junit.Test;
import java.util.Date;

/**
 * Test class for {@link Review}.
 * <p>
 * This class contains unit tests for verifying the functionality of the Review class,
 * including constructor variants, property getters/setters, and validation rules.
 * </p>
 * 
 * @author CSE360 Team
 * @version 1.0
 * @see Review
 */
public class ReviewTest {

    /**
     * Default constructor for ReviewTest.
     * <p>
     * Initializes a new test instance for testing Review functionality.
     * </p>
     */
    public ReviewTest() {
        // Default constructor
    }

    // Rest of your existing methods...

    // For the methods with errors, remove the extra </p> tags
    // Example fix (adjust according to actual code):
    /**
     * Tests the validation rules for the updatedAt timestamp.
     * <p>
     * Verifies that:
     * <ul>
     *   <li>Future dates are accepted</li>
     *   <li>Dates before creation date are rejected</li>
     *   <li>Null dates are rejected</li>
     * </ul>
     * </p> <!-- This is the problematic tag that should be removed -->
     */
    // The correct version would be:
    /**
     * Tests the validation rules for the updatedAt timestamp.
     * <p>
     * Verifies that:
     * <ul>
     *   <li>Future dates are accepted</li>
     *   <li>Dates before creation date are rejected</li>
     *   <li>Null dates are rejected</li>
     * </ul>
     * </p>
     */
}